{% extends 'base.html.twig' %}

{% block title %} Tasks {% endblock %}

{% block body %}

<div class="example-wrapper">
    <p>Hello, world!</p>
    {{ form_start(form, {'action': '/task', method: 'GET', 'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
